
    let ab = +prompt('Введите число')
    let ba = +prompt('Введите степень')
if (ab == 0 || ba === 0) {
    alert( '0 ')
    
}else if( isNaN(ab) && (ba) || isNaN(ba) && (ab) ) {
    alert('Ошибка')
}else if((ab > 0) || (ba > 0)) {
    alert (ab  + ' в '  + ba  + ' степени = ' + (ab ** ba))
}else if( isNaN(ab) || isNaN(ba)) {
    alert(' Ошибка ')
}else if( isNaN(ab) || isNaN(ba)) {
    alert (' Ошибка ')
}